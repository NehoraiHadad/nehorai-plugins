# @nehorai/payments-paypal

## 0.2.1

- Ship the `LICENSE` file (MIT) in the published package. No code changes —
  the built output is bit-identical to 0.2.0.

## 0.2.0

- Subscriptions: implement the optional `ISubscriptionProvider` seam
  (products, billing plans, create/cancel subscription, webhook events).

## 0.1.0

- Initial release: PayPal Orders v2 provider adapter for `@nehorai/payments`
  (create/capture/verify, webhook signature verification, real refunds).
